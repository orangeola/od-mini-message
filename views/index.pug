extends layout

block content
  h1= title
  p Welcome to #{title}
  <a href="/new">Add a message!</a>
  ul
    each message in messages
      div
        p= message.text
        p= message.user
        p= message.added
        br

